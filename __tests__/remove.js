const sinon = require('sinon')
const plugin = require('../plugin')

test('removes ReactNativeNavigation', async () => {
  expect(true).toEqual(true)
//   const removeModule = sinon.spy()
//   const removePluginComponentExample = sinon.spy()
// 
//   const context = {
//     ignite: { removeModule, removePluginComponentExample }
//   }
// 
//   await plugin.remove(context)
// 
//   expect(removeModule.calledWith('react-native-MODULENAME', { unlink: true })).toBe(true)
//   expect(removePluginComponentExample.calledWith('ReactNativeNavigationExample.js')).toBe(true)
})
