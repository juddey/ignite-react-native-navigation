import { Navigation } from 'react-native-navigation'
import Welcome from '..<%= props.location %>/welcome'

function registerScreens () {
  Navigation.registerComponent('<%= props.name %>.Welcome', () => Welcome)
}
export { registerScreens }
