const singleScreenApp = {
  root: {
    stack: {
      children: [{
        component: {
          name: '<%= props.name %>.Welcome',
          passProps: {
            text: 'Hello, I am Awesome'
          }
        }
      }],
      options: {
        topBar: {
          title: {
            text: 'Welcome screen'
          }
        }
      }
    }
  }
}

export { singleScreenApp }